# Arquivo de exemplo de variáveis de ambiente (não contém segredos)
# Copie para ".env" e preencha os valores seguros para produção.

# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=seu-dominio.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://seu-dominio.com

# Banco de Dados
# Para SQLite (desenvolvimento):
# DB_ENGINE=django.db.backends.sqlite3
# Para MySQL (produção):
DB_ENGINE=django.db.backends.mysql
DB_NAME=seu_banco
DB_USER=seu_usuario
DB_PASSWORD=seu_password
DB_HOST=seu_host
DB_PORT=3306
# Caminho do certificado CA se necessário (deixe vazio para usar default do sistema ou o arquivo incluso)
DB_SSL_CA=

# Conexões persistentes (0 desativa)
DB_CONN_MAX_AGE=0# Django
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CSRF_TRUSTED_ORIGINS=

# Database (choose one)
# SQLite (default) - no vars needed

# MySQL
DB_ENGINE=django.db.backends.mysql
DB_NAME=tutorialdjangodb
DB_USER=maristela
DB_PASSWORD=<your-password>
DB_HOST=stelaserver.mysql.database.azure.com
DB_PORT=3306
# Path to SSL CA bundle; default uses repo path
DB_SSL_CA=

# Optional DB connection pooling (seconds). 0 disables reuse.
DB_CONN_MAX_AGE=0
